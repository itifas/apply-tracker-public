# Scripts

This folder is reserved for frontend-only utility scripts.

Current state:

- No custom scripts are checked in yet.

Related commands:

- Prisma seed runs from `frontend/`: `npm run prisma:seed`
- Docker lifecycle scripts are in `../helpers/docker/`
